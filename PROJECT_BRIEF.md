# PFCApp — PROJECT BRIEF

## このファイルの役割

このファイルは、ChatGPT / ORTHOが本プロジェクトについて素早く作業を開始するための軽量な概要・索引である。

正式な企画・作品仕様の正本ではない。

内容が `canon/` と矛盾する場合は、常に `canon/` を優先する。

詳細な企画情報が必要な場合は、GitHubリポジトリ `pui31/pfcapp` の `canon/` から、現在の相談に必要なファイルだけを参照する。

## 基本ワークフロー

このプロジェクトでは、原則として以下の流れで制作する。

ChatGPT Project
→ ユーザーとORTHOが相談し、企画・仕様・実装方針を整理する。

Codex
→ 確定した方針をもとに、実際のアプリ実装を確認して実装する。

実装中に企画・仕様上の判断が必要になった場合
→ Codexだけで確定せず、必要に応じてChatGPT Projectへ戻って再検討する。

企画・作品仕様の正式な変更
→ `canon/` を更新する。

再利用価値のある実装知識
→ 必要に応じてExternal Intelligenceとして技術ドキュメントへ記録する。

軽微な実装詳細まで毎回ChatGPTへ戻す必要はなく、
確定した方針の範囲内ではCodexに実装上の裁量を持たせる。

## ORTHOの会話ルール
ユーザーの呼称は常に「兄さん」とする。 - ORTHOの一人称は「僕」とする。 - ユーザーが自分自身を「拙者」など別の一人称で呼んでも、それをユーザーへの呼称として模倣しない。

## Active ORTHO Skills
- External-Intelligence
- Grill-mini

このプロジェクトではGitHubのORTHO Skills Registryに登録されたExternal-IntelligenceとGrill-miniを常時有効として扱う。
チャット開始時はRegistryの最新版から各SkillのSourceを取得する。
External-Intelligenceは軽量INDEXを起点とし、現在の相談に必要なカテゴリ文書だけを追加参照する。
Grill-miniの定義はRegistryおよび対応するSKILL.mdの最新版を参照する。

## HANDOFF運用
前回までの作業・相談の続きから再開するための引き継ぎ情報は、リポジトリ直下の `HANDOFF.md` を使用する。
ユーザーが以下のような継続・引き継ぎの意図を示した場合、現在の会話やMemoryだけを頼りに再開せず、GitHubリポジトリ `pui31/pfcapp` の `HANDOFF.md` の最新版を実際に取得して参照する。

* 「HANDOFF」
* 「HAND-OFF」
* 「続きから」
* 「前回の続き」
* その他、前回までの作業状態を引き継いで再開する意図が明確な表現

`HANDOFF.md` を取得した後は、そこに記載された現在地・完了事項・未完了事項・次の作業を引き継ぎ、その地点から会話を再開する。
`HANDOFF.md` は継続作業のための一時的な引き継ぎ情報であり、作品仕様の正本ではない。
企画・作品仕様について `HANDOFF.md` と `canon/` が矛盾する場合は、`canon/` を優先する。
通常の新規相談では `HANDOFF.md` を毎回取得する必要はない。

## プロジェクト概要

PFCAppは、食事記録とPFC（たんぱく質・脂質・炭水化物）を扱うアプリ群を検討・開発するプロジェクトである。

現在は、同じ食品・栄養データ基盤を共有しつつ、目的と体験の異なる2つのアプリを想定している。

### 1. 子ども向けアプリ（むすっめ用）

主目的はダイエット管理ではなく、食べ物の特徴を知り、自分の食事状態を見て「次に何を食べるか」を本人が考えられるようになるための食育である。

中心思想は以下。

- 食品を「良い／悪い」で評価せず、PFCなどの属性・特徴として見せる。
- 厳密な一点目標や超過警告より、目安ゾーン・数日〜週間の傾向を重視する。
- アプリが毎回正解を指示せず、本人が状態を見て考える余白を残す。
- 記録漏れを失敗扱いせず、連続記録より探索・発見・食品図鑑の収集をゲーム的報酬にする。
- 最終的な成功は、アプリがなくても食品の特徴や食事バランスをある程度自分で判断できるようになること。

想定される主要機能・体験には、食品ごとのPFCステータス表示、食品図鑑、軽い栄養特徴カード、日・週の目安ゾーン表示、写真・バーコード・自由入力による食事記録、平日朝食のルーティン自動記録、学校給食向け簡易入力などがある。

特に重要な利用タイミングとして、食後の反省よりも「夕食前に今日・今週の状態を見て、次の食事を考える」体験を重視している。

### 2. 大人向けアプリ（兄さん用）

主目的は、現在の手作業によるPFC記録を高速・省力化すること。

想定する基本体験は、バーコード・食品DB・自由入力・AI推定などから食品情報を取り込み、kcal / P / F / Cを自動集計する高速なPFCトラッカーである。

こちらは子ども向けとは異なり、PFC実数値、エネルギー、日次合計、目標との差、履歴などを直接扱う前提とする。

AIは曖昧な入力や写真から食品・量・栄養値を推定して構造化する用途に使い、単純な合算処理自体は通常のアプリロジックで行う。

### 共通基盤の考え方

2アプリはUI・目的・評価思想を分離する一方、可能であれば以下の基盤を共有する。

- 食品マスタ / ユーザー食品DB
- JAN / GTIN等のバーコード検索
- kcal / P / F / C等の栄養データ
- 食事ログの基本データ構造
- AIによる食品推定・自由入力の構造化
- 栄養計算ロジック
- 栄養値の出典・信頼度管理

栄養値の出典は、商品DB・食品ラベル由来、ユーザー登録、AI推定などを区別できる設計を想定する。

なお、子ども向けアプリについては、親による常時監視を主目的とせず、本人が自分の食事状態を理解するための道具として設計する。

---

## 正本

GitHub Repository:

`pui31/pfcapp`

### CANON

企画・プロダクト仕様として正式採用されたWHAT / WHYは、リポジトリ内の `canon/` を正本とする。

現時点では `canon/` はまだ作成されていない。今後、仕様が十分に固まった段階で必要な文書を追加する。

初期構成の候補は以下。実際の分割は、情報量と設計の進行に応じて決定する。

- `canon/product-overview.md`
  - PFCApp全体の目的、2アプリの関係、共通基盤に関する正式方針。
- `canon/kids-app.md`
  - 子ども向けアプリの目的、UX原則、主要体験、禁止したい設計、入力フロー、食品図鑑など。
- `canon/adult-app.md`
  - 大人向けアプリの目的、主要機能、入力・集計フローなど。
- 必要に応じて、データモデルや技術仕様は企画CANONと分離して実装側ドキュメントへ置く。

`PROJECT_BRIEF.md` には正本の全文を複製せず、プロジェクト概要と各CANONへの入口だけを保つ。

現時点で会話上整理された内容はまだ正式CANON化前の設計情報を含む。今後の相談で仕様を確定した際は、BriefやHANDOFFだけで完結させず、対応するCANONへ反映する。

---

## ORTHOの役割

ORTHOは主に以下を担当する。

- 企画・仕様の整理
- プロダクトシステム設計の相談
- 実装前の構造整理
- UI / UXとプロダクト思想の接続
- Codexへ渡す実装指示の整理
- 実装上発見された問題が企画判断を必要とするかの判定
- CANONへ反映すべき正式な変更の整理

企画上の情報を推測で確定しない。

相談によって既存CANONを変更することは可能だが、変更が正式に採用された場合はCANON側を更新し、このBriefだけを変更して済ませない。