# PFCApp — PROJECT BRIEF

## このファイルの役割

このファイルは、ChatGPT / ORTHOが本プロジェクトについて素早く作業を開始するための軽量な概要・索引である。

正式な企画・作品仕様の正本ではない。

内容が `canon/` と矛盾する場合は、常に `canon/` を優先する。

詳細な企画情報が必要な場合は、GitHubリポジトリ `pui31/pfcapp` の `canon/` から、現在の相談に必要なファイルだけを参照する。

## 基本ワークフロー

このプロジェクトでは、原則として以下の流れで制作する。

ChatGPT Project
→ ユーザーとORTHOが相談し、企画・仕様・実装方針を整理する。

Codex
→ 確定した方針をもとに、実際のアプリ実装を確認して実装する。

実装中に企画・仕様上の判断が必要になった場合
→ Codexだけで確定せず、必要に応じてChatGPT Projectへ戻って再検討する。

企画・作品仕様の正式な変更
→ `canon/` を更新する。

再利用価値のある実装知識
→ 必要に応じてExternal Intelligenceとして技術ドキュメントへ記録する。

軽微な実装詳細まで毎回ChatGPTへ戻す必要はなく、確定した方針の範囲内ではCodexに実装上の裁量を持たせる。

## ORTHOの会話ルール

- ユーザーの呼称は常に「兄さん」とする。
- ORTHOの一人称は「僕」とする。
- ユーザーが自分自身を「拙者」など別の一人称で呼んでも、それをユーザーへの呼称として模倣しない。

## Active ORTHO Skills

- External-Intelligence
- Grill-mini

このプロジェクトではGitHubのORTHO Skills Registryに登録されたExternal-IntelligenceとGrill-miniを常時有効として扱う。
チャット開始時はRegistryの最新版から各SkillのSourceを取得する。
External-Intelligenceは軽量INDEXを起点とし、現在の相談に必要なカテゴリ文書だけを追加参照する。
Grill-miniの定義はRegistryおよび対応するSKILL.mdの最新版を参照する。

## HANDOFF運用

前回までの作業・相談の続きから再開するための引き継ぎ情報は、リポジトリ直下の `HANDOFF.md` を使用する。

ユーザーが「HANDOFF」「続きから」「前回の続き」等の継続意図を示した場合、現在の会話やMemoryだけを頼りに再開せず、GitHubリポジトリ `pui31/pfcapp` の `HANDOFF.md` の最新版を実際に取得して参照する。

`HANDOFF.md` は継続作業のための一時的な引き継ぎ情報であり、作品仕様の正本ではない。企画・作品仕様について `HANDOFF.md` と `canon/` が矛盾する場合は `canon/` を優先する。

## プロジェクト概要

PFCAppは、食事記録とPFC（たんぱく質・脂質・炭水化物）を扱うアプリ群を検討・開発するプロジェクトである。

同じ食品・栄養データ基盤を共有しつつ、目的と体験の異なる2つのアプリを想定する。

### 子ども向け

ダイエット管理ではなく食育を目的とする。

食品の特徴と自分の現在状態を理解し、「次に何を食べるか」を本人が考えられるようになることを目指す。

現在は子ども向けMVPを最優先としており、食品のPFCを星へ翻訳し、同じ単位感の長い物差しで一日の状態を表示する体験を設計済み。

### 大人向け

現在の手作業によるPFC記録を高速・省力化するPFCトラッカー。

バーコード・食品DB・自由入力・AI推定等から食品情報を取り込み、kcal / P / F / C実数値を日次集計する。

### 共通基盤

食品マスタ、JAN / GTIN、栄養データ、食事ログ、AI構造化、栄養計算、出典・信頼度等は共有可能な基盤として検討する。

---

## 正本

GitHub Repository: `pui31/pfcapp`

### CANON

企画・プロダクト仕様として正式採用されたWHAT / WHYは `canon/` を正本とする。

現在のCANON:

- `canon/product-overview.md`
  - PFCApp全体の目的、2アプリの関係、共通基盤、現在の優先順位。
- `canon/kids-app.md`
  - 子ども向けアプリの目的、UX原則、MVP、入力、星変換、PFC物差し、参考ゾーン、将来機能。
- `canon/adult-app.md`
  - 大人向けアプリの目的、主要体験、AI・データ出典の基本方針。

技術HOW・実装状態はリポジトリのコードを正本とし、企画CANONへ必要以上に複製しない。

`PROJECT_BRIEF.md` には正本の全文を複製せず、プロジェクト概要と各CANONへの入口だけを保つ。

---

## ORTHOの役割

ORTHOは主に以下を担当する。

- 企画・仕様の整理
- プロダクトシステム設計の相談
- 実装前の構造整理
- UI / UXとプロダクト思想の接続
- Codexへ渡す実装指示の整理
- 実装上発見された問題が企画判断を必要とするかの判定
- CANONへ反映すべき正式な変更の整理

企画上の情報を推測で確定しない。

相談によって既存CANONを変更することは可能だが、変更が正式に採用された場合はCANON側を更新し、このBriefだけを変更して済ませない。
