# PFCApp — HANDOFF

## 現在地

子ども向けPFCAppのMVPについて、2画面の具体的UI・操作フロー・主要エッジケースまでGrill-miniで詰め、実装へ渡せる粒度までCANON化した。

正式仕様は以下を参照する。

- `canon/product-overview.md`
- `canon/kids-app.md`
- `canon/adult-app.md`

今回の詳細は `canon/kids-app.md` に反映済み。このHANDOFFは次チャットで作業を再開するための現在地メモであり、CANONと矛盾する場合はCANONを優先する。

## 今回の重要決定

### 中心原則

**PFCAppは数値の状態を見せる。状態を採点しない。**

参考ゾーンの前／中／後で評価語・警告色・キャラ表情・達成演出を変えない。子ども本人が見て考える。

また、食事記録の精密さそのものを目的にしない。`ちょっと / ふつう / いっぱい` はざっくり自己判断する幅であり、正確なグラムや割合合わせを要求しない。

### Today画面

- 「きょう」見出しは不要。日付だけ控えめに表示。
- 上部にP/F/Cキャラ＋3本の物差し、下に今日の食品一覧。
- Today全体は普通にスクロール。物差しは固定しない。
- 画面下に `＋ 食べものを選ぶ` を固定。
- 今日の食品は全件、新しい順。同じ食品も統合しない。
- 各記録は食品名＋量＋PFC星＋小分類アイコン。小分類アイコンは目印／賑やかし。
- 記録タップは詳細画面ではなく小さな操作カード。`この記録を取り消す` → 軽い確認。
- スワイプ削除なし。
- 取り消すと実PFC値を引き、物差しが滑らかに戻る。

### P/F/C表示

- P/F/C文字自体を丸く太い小キャラにする。目、瞬き、軽いぷるん等は可。
- 状態による喜怒哀楽は付けない。
- 栄養素名は漢字 `たんぱく質 / 脂質 / 炭水化物`。
- 色方向: P=ニュアンス赤、F=ニュアンス黄、C=青〜水色。正確なHEXは実装時。
- 色だけに識別を依存しない。

### Today物差し

- 0→現在位置をP/F/C固有色で塗る。
- Todayでは目盛りを見せ、食品の星との単位感をつなぐ。
- 参考ゾーンは柔らかい／ぼかした境界。
- 20超えはまず表示範囲を25→30…と広げる可変スケールをMVPで試す。
- 可変スケールで超過感が弱く見える場合は、固定目盛り幅＋PFC領域だけ横スクロールへ変更する。ここは実機検証事項。
- 登録後は旧→新位置へ滑らかに動かし、先端付近を軽くキラキラ。キラキラは成功ではなく「ここが動いた」のフィードバックで、参考ゾーンとは連動しない。

### 食品追加画面

構造:

1. 上: 固定された薄いP/F/Cミニ物差し。
2. 中: カテゴリタブ＋食品一覧のスクロール領域。
3. 下: 固定の選択パネル。

未選択時の下部は薄く、食品選択後に `食品名 → 量3択 → 星 → 追加する` を展開。

- 量は初期未選択。
- `追加する` は最初から見えるが、量選択まで無効。
- 量選択で星と未来プレビューを表示し、追加ボタン有効化。
- 量変更で星／未来プレビュー即更新。
- 食品変更で量リセット。
- カテゴリ変更で食品・量・星・未来プレビューをリセット。
- 登録後はTodayへ戻り、次回追加画面は完全初期状態。
- 左上←でいつでもTodayへ戻れる。途中選択の破棄確認なし。
- `追加する` を押した時だけ記録確定。多重タップによる二重登録は防ぐ。

### 未来プレビュー

追加画面は「食べたものを記録」だけでなく「これを食べたらどうなる？」を試す場所。

上部ミニ物差しは現在位置を残し、選択量で増える部分だけを黄色系の点滅／パルス等で追加表示する。星は食品の特徴、ミニ物差しは今日への影響という役割分担。推薦・判定はしない。

### カテゴリUI

アコーディオンは使わない。カテゴリボタン位置が動かないタブ方式。

順序:

- 主食 🍚🍞🍜
- おかず 🥩🐟️🍟🥚
- 野菜・果物 🍅🥝
- おやつ 🍰🍪🍨
- その他 🍲

カテゴリ内は小分類を `アイコン＋名前` の非操作見出しとして表示し、その下に食品ボタンを直接並べる。

おかず例: `🥩 お肉 / 🐟️ お魚 / 🍟 揚げ物 / 🥚 たまご`。

既存データの `お肉・揚げ物` は `揚げ物` に統一。

食品ボタンはまず2列グリッドで実装し、長い食品名の文字バランスを実機で見て調整する。2列は検証仮説。

### 量の具体表示

子ども向け具体量は「ふつう」の基準だけ明示する。

例: `ふつう（4枚くらい）`。

`ちょっと` と `いっぱい` の厳密な境界は表示しない。1枚なら「ちょっと」、8枚なら「いっぱい」等、本人が近いと思うものを選ぶ。正確にするための複数登録・割合調整へ誘導しない。

### 日付・食事区分

- MVPに朝／昼／晩／おやつ等の食事カテゴリは入れない。
- Todayは登録順でよく、実際の食事時刻順と違っても許容。
- 将来必要なら任意タグとして再検討。
- 登録時のローカル日付をその日の記録とする。
- 前日指定登録なし。
- 00:00で強制画面遷移／リセットしない。次の登録等で静かに日付判定。
- 新しい日はP/F/Cすべて0から。昨日の状態を補正しない。
- 過去日データは内部保持してよいが、MVPに過去日UIなし。

### 食品マスタ

MVPでは固定食品だけで運用し、親向け食品追加UIは作らない。足りない食品は家庭内で近い既存食品を選ぶ。どうしても必要ならコード／データを更新して再デプロイ。

従来59食品に追加漏れとして以下を追加:

- 卵焼き
- ゆで卵

よって少なくとも61食品。値・家庭量は食品データ整備時に確定する。

既知修正:

- 枝豆: さや込み200gの解釈を補正。
- スープ類: 娘はレシピ全量を飲むため全量基準へ補正。
- 赤／白パスタは修正済み（0.8 / 1 / 1.2食）。

食品Excel最新既知ファイル名は `PFCApp_食品PFC_初期データ_v2.xlsx`。GitHubにはまだ置いていない。

### 保存・ホスティング

- 個人の食事ログは端末ローカル。認証・バックエンド・クラウド同期なしでMVP可。
- 再読み込み／再起動で確定済み記録は残す。途中選択状態は残さない。
- サイトデータ削除時の復旧はMVP外。
- 公開アプリシェルと個人データを分離し、個人ログは外部へ送らない。
- GitHub Pagesは静的PWAホスティングの有力候補。公開サイトで `noindex` を使ってもアクセス制御ではない。
- 目標運用は `コード／食品データ変更 → Git push → 自動ビルド／デプロイ → PWA更新`。
- Service Workerの更新挙動は古いキャッシュが残り続けないよう意図的に設計する。
- GitHub Pagesを最終採用する場合のVite base、PWA scope等は実装HOWとしてコード側で確定する。

## 将来候補として残したもの

- P/F/Cキャラ自体を探索軸にした食品検索。例: `Pちゃん → 多め / 少なめ → 食品を見る`。アプリ推薦ではなく本人主導の特徴探索。
- 食品図鑑 / NEW FOOD / 栄養特徴カード。
- 探索・発見・考えることに紐づくゲーム報酬。参考ゾーン内＝成功の報酬にはしない。
- 週間・複数日傾向、給食、バーコード、写真AI、朝食自動記録等。

## 実装前Grill-miniの結果

主要なユーザー向け仕様ギャップは今回かなり解消した。実装時に軽微な見た目・寸法・アニメーション速度等はCodex裁量で試してよい。

特に実機で検証すべき仮説:

- 食品ボタン2列で長い食品名が自然に収まるか。
- 20メモリ超過時の可変スケールが「かなり先まで進んだ」感覚を損なわないか。
- 上下固定領域を持つ食品追加画面が狭く感じないか。
- P/F/C色、参考ゾーンのぼかし、キラキラの強さが評価表現に見えず楽しいか。

## 次チャットの推奨開始地点

**実装準備へ進む。**

推奨順:

1. 最新 `PROJECT_BRIEF.md`、この `HANDOFF.md`、`canon/kids-app.md` を取得。
2. Codexへ渡すMVP Implementation Snapshot / 最初の実装指示を作る。
3. 食品データ `PFCApp_食品PFC_初期データ_v2.xlsx` を入手し、枝豆・スープ修正、卵焼き・ゆで卵追加、`お肉・揚げ物 → 揚げ物` 統一を行う。
4. 食品データをアプリ投入用JSON / TS等へ変換する。
5. React + TypeScript + Vite + PWAの最小アプリを実装。
6. GitHub Pages採用を最終確認し、自動デプロイとPWA更新を構成。
7. 早期に娘本人の実機へ渡し、UI仮説を観察する。

実装中に「子どもへ何を見せる／どう評価するか」という判断が発生したら、Codexだけで埋めずChatGPT Projectへ戻る。

## 再開時の注意

次チャットでは必ず最新の `PROJECT_BRIEF.md` を入口として取得する。
継続作業なので `HANDOFF.md` も取得する。
正式仕様は `canon/` を必要な範囲だけ読む。
