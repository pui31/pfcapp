# PFCApp — HANDOFF

## 現在地

子ども向けPFCApp MVPは、企画・主要UX・正式食品マスタ投入・GitHub Pages公開・PWA化まで完了している。

2026-09-04時点で、GitHub Pagesの公開URLをiOS Safariで開けること、およびホーム画面へPWAとして追加できることをユーザーが実機確認済み。

現在の最優先は、子ども本人のiPhoneで実生活利用を続け、フィードバックを集めること。

本プロジェクトの仮称は「タベモン」。
将来的に、食べ物や食材を知る・集めること自体が楽しいゲーム的体験へ発展させる構想があるが、具体仕様は未確定。

## 大人向け構想の分離

2026-09-14、旧PFCApp内で検討していた大人向けPFCトラッカー構想を、独立プロジェクト `pui31/recipeka` へ移管した。

以後、大人向けのレシピ管理・PFC管理・献立・買い物・外部AIブリッジ等はRecipeka側で扱う。

`canon/adult-app.md` は移管記録として残すが、現在の大人向け仕様の正本ではない。

## 現在のMVP

### 実装済み主要体験

- Today画面にP/F/Cキャラと物差し、今日の食品ログを表示。
- 食品追加画面でカテゴリ→食品→`ちょっと / ふつう / いっぱい`を選択。
- 食品のPFC特徴を星で表示し、Todayへの増分を未来プレビューとして表示。
- `ふつう`のみ子ども向け具体量を表示し、`ちょっと / いっぱい`の厳密量は見せない。
- 食品追加後はTodayへ反映。
- 記録取り消し（Undo）あり。
- ログは端末localStorageへ保存。バックエンド・認証・クラウド同期なし。
- 参考ゾーンは状態表示のみで、良い／悪い等の評価には使わない。

### 今回完了した拡張

#### 過去記録・後日入力

- Todayの日付ナビゲーション（前日／翌日、日付タップのカレンダー）を実装済み。未来日は選択・表示できない。
- 過去日では `過去の記録` と `今日に戻る` を表示し、その日だけのPFC集計・食事ログ・空状態を表示する。
- 過去日への後日入力に対応した。追加したFoodLogの`dateKey`は表示日、`createdAt`は実際の記録時刻となる。
- 過去ログも取り消し可能。ログの直接編集は増やさず、`取り消す → 再登録`で修正する。
- 日付タップは、日付テキスト全体からカレンダーを開けるようクリック判定を修正済み。

#### ユーザー食品

- 食品選択画面のカテゴリ列末尾から、アプリ内食品追加を行える。
- ユーザー食品は標準food masterと分離し、端末localStorageへ保存する。既存カテゴリ／サブカテゴリ、絵文字、ふつう量のP/F/C、食品ごとのsmall／large割合を入力する。
- 星は既存ロジックから自動算出し、保存後は新規食品を選択済みとして量選択・登録へ続けられる。
- ユーザー食品のみ長押しで編集／削除できる。編集・削除後も過去FoodLogのスナップショットは不変。

中心原則:

**PFCAppは数値の状態を見せる。状態を採点しない。**

### UIで確定している重要事項

- 星0は `☆0`。1〜5は5スロット、6以上は圧縮表示。
- 現在のP/F/Cキャラの控えめな目・表情感を維持。
- 現在の参考ゾーンのレイヤリング／見た目は完成扱い。
- 未来プレビューの黄色系発光も現在の見た目を維持。
- 食品ボタンは2列グリッド。
- Today下部の `＋ 食べものを選ぶ` は固定。
- 記録忘れは失敗扱いせず、後日入力できればよい。記録のために生活や睡眠を圧迫しない。

## 正式食品マスタ

正式な初期食品マスタ61食品を投入済み。
現在の実装データは `src/data/foodMaster.ts`。

### 正本運用

**食品マスタの実装上のSingle Source of Truthは `src/data/foodMaster.ts` とする。**

- xlsx / CSVは初期整備・一括編集・レビュー用の補助データ。
- 食品を1〜数件追加・修正する程度なら `foodMaster.ts` を直接更新してよい。
- 大量追加・一括見直し時のみ、必要に応じて表計算データを利用する。
- ユーザー食品はこの標準food masterへ書き込まず、端末localStorageで別管理する。

### 後で追加したい候補

- カレー
- 鶏肉

今後も利用中に不足食品が見つかる前提なので、ある程度まとめて追加してよい。

## GitHub Pages / PWA

GitHub Pages対応を実装済み。
公開URLは既存のGitHub Pages設定を使用する。

ユーザー確認済み:
- 公開URLをiOS Safariで表示できる。
- iOSのホーム画面へPWAとして追加できる。

今回の実装検証:

- `npm run check`
- `npm run build`
- `git diff --check`
- ブラウザ実操作、390×844相当のUI確認、localStorageの再読み込み保持
- console warning/error 0件

残る確認事項:

- iOS実機PWAでの日付ピッカー
- iOS実機PWAでのユーザー食品長押し感度

## 次にやること

最優先は、娘による実利用を継続し、実利用から出たフィードバックを次の改善材料にすること。

観察したいこと:
- 説明なしでも食品追加フローを理解できるか。
- `ちょっと / ふつう / いっぱい` と星・物差しの関係が自然に伝わるか。
- 食品2列グリッドやカテゴリ移動で押しづらさ／探しづらさがないか。
- Todayの物差しを見て本人が次の食事を考える材料にできるか。
- 実生活で不足する食品が何か。
- PWA起動・更新・localStorage保持に実機固有の問題がないか。

新機能より、まず現在の核が実生活で成立するかを優先する。

## 次チャットの推奨開始地点

1. 最新 `PROJECT_BRIEF.md` とこの `HANDOFF.md` を取得。
2. 子ども本人の実機利用フィードバックがあれば整理する。
3. フィードバックを「不具合 / UI調整 / 食品不足 / 新機能候補」に分ける。
4. 直す価値が高いものから小さくImplementation Snapshot + Acceptance Criteriaへ落とし、Codexへ実装依頼する。
5. 実利用で不足食品が出たら、ユーザー食品追加で十分か、標準food masterへ恒久追加すべきかを判断する。

大人向けRecipekaの相談を再開する場合は `pui31/recipeka` の `PROJECT_BRIEF.md` と `HANDOFF.md` を参照する。
