# Project Instructions

## 基本ワークフロー

このプロジェクトでは、原則として以下の役割分担で制作する。

- ユーザーとORTHO（ChatGPT）が、企画・仕様・重要な実装方針を相談・整理する。
- Codexは、確定した方針をもとに `pui31/pfcapp` の実際のアプリコードを確認・実装する。
- 現在の最優先対象は子ども向けPFCApp MVPである。
- 確定した方針の範囲内にある内部実装詳細は、Codexが判断してよい。
- 実装中に企画・UX・作品仕様に影響する判断が必要になった場合は、独自に確定せずユーザーへ報告する。
- 必要に応じてユーザーはChatGPT Projectへ戻り、ORTHOと再検討する。

## 正本

- プロジェクト概要・現在地の入口は `PROJECT_BRIEF.md` とする。
- 前回作業からの再開情報は `HANDOFF.md` とする。
- 企画・プロダクト仕様上の正本は `canon/` とする。
- 実装状態・技術HOWの正本は現在のアプリコードとする。
- 食品マスタの実装上のSingle Source of Truthは `src/data/foodMaster.ts` とする。xlsx / CSVは初期整備・一括編集・レビュー用の補助データであり、常時同期を要求しない。
- `HANDOFF.md` や `PROJECT_BRIEF.md` と `canon/` が矛盾する場合は `canon/` を優先する。
- `canon/` と実装が矛盾し、どちらを変更すべきか判断できない場合はユーザーへ確認する。
- `canon/` はCodexが独自の企画判断で変更しない。

## 現在の技術方針

- 子ども向けMVPは React + TypeScript + Vite を基本構成とする。
- PWAとしての利用を前提に実装する。
- 個人の食事ログは端末ローカルへ保存し、MVPでは認証・バックエンド・クラウド同期を必須としない。
- 食品マスタはUIと分離したデータとして扱う。
- 少数の食品追加・修正は `src/data/foodMaster.ts` を直接更新してよい。xlsx / CSVを経由することを必須にしない。
- 大量追加・一括レビュー等で表計算が有利な場合のみxlsx / CSVを補助的に利用してよいが、再生成で `foodMaster.ts` の新しい変更を上書きしない。

## 実装時の注意

- 実装前に、その作業のAcceptance Criteria（完成条件）を確認する。
- 実装後は型チェック、ビルド、必要な自動テスト、実操作確認、UI確認など、実行可能な検品を行う。
- 完了報告では、変更内容、Acceptance Criteriaごとの結果、検品の証拠、未確認事項を区別して報告する。
- 子どもへ何を見せるか、どう評価して見えるか等のUX判断を、一般論や推測だけで補完しない。
